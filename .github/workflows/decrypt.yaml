name:  Decrypt File

on: push

jobs:
  decrypt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Decrypt File
        env:
          MY_SECRET: ${{ secrets.MY_SECRET }}
        run: |
          gpg --quit --bash --yes --decrypt --passphrase="$MY_SECRET"--output=$HOME/my_secret.json my_secret.json.gpg
      - name: Print file content
        run: cat $HOME/my_secret.json